# Automatically added by dh_installinit
if [ -x "/etc/init.d/zionX" ]; then
	if [ ! -e "/etc/init/zionX.conf" ]; then
		update-rc.d zionX defaults >/dev/null
	fi
	invoke-rc.d zionX start || exit $?
fi
# End automatically added section
